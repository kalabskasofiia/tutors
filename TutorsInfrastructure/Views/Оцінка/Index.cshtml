@model IEnumerable<TutorsDomain.Model.Оцінка>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Бал)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.МаксимальнийБал)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Assigment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gradebook)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Бал)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.МаксимальнийБал)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Assigment.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gradebook.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
